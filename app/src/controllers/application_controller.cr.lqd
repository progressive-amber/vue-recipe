require "jasper_helpers"

class ApplicationController < Amber::Controller::Base
  include JasperHelpers
  LAYOUT = "application.{{ language }}"

  def root
    render "application.{{ language }}"
  end
end
